name: channel_monitor
description: monitor flutter platform channel profiler
version: 0.0.3
homepage: https://github.com/siyehua/flutter_channel_monitor

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  path_provider: ^2.0.4
  intl: ^0.17.0
  charts_flutter: ^0.11.0
  file_picker: ^4.1.6
dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

